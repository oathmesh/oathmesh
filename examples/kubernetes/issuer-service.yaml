apiVersion: v1
kind: Service
metadata:
  name: oathmesh-issuer
  namespace: oathmesh
  labels:
    app.kubernetes.io/name: oathmesh
    app.kubernetes.io/component: issuer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: oathmesh
    app.kubernetes.io/component: issuer
  ports:
    - name: http
      port: 4000
      targetPort: http

